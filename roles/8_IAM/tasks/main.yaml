- name: "Create IAM Role For Access S3 From Instance"
  iam_role :
    name: "{{item.name}}"
    region: "{{ default_region }}"
    assume_role_policy_document: " {{ lookup( 'file', 'template/s3_policy.json') }} "  처리가 안됨.
  loop: "{{aws_iam_role}}"